# Copy to .env (stays local, gitignored) or export in your shell.
# Must be at least 32 characters. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SAATHI_JWT_SECRET=replace-with-a-long-random-string-min-32-chars
